import { Card } from "../ui/card"

type TeamCardProps = React.HTMLAttributes<HTMLDivElement> & {

}

export const TeamCard: React.FC<TeamCardProps> = ({ className }) => {

   return (
      <Card>

      </Card>
   )
}